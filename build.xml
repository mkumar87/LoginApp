<?xml version="1.0"?>
<project name="LoginApp" default="test" basedir="."> 
  <target name="init"> 
    <tstamp/> 
  </target>
<property name="build" 	value="build" />
<target name="prepare" depends="init" description="Create build directories.">
  <mkdir dir="${build}" />
  <mkdir dir="${build}/WEB-INF" /> 
  <mkdir dir="${build}/WEB-INF/classes" />
</target>

<!-- Executable Targets -->

<target name="install" description="Install Web application"
    depends="test">
  <install url="${url}" username="${username}" 
      password="${password}" path="${path}" 
war="file:${build}"/> 
</target>

<target name="reload" description="Reload Web application"
      depends="test">
  <reload url="${url}" username="${username}" 
      password="${password}" path="${path}"/>
</target>

<target name="remove" description="Remove Web application"> 
<remove url="${url}" username="${username}" 
      password="${password}" path="${path}"/>
</target>

<target name="test" depends="prepare" description="Compile app Java files and copy HTML and JSP pages" >
  <javac srcdir="src" destdir="${build}/WEB-INF/classes"> 
    <include name="**/*.java" /> 
  </javac> 
  <copy todir="${build}/WEB-INF"> 
    <fileset dir="WebContent/WEB-INF" >
      <include name="web.xml" />
    </fileset> 
  </copy> 
  <copy todir="${build}"> 
    <fileset dir="WebContent"> 
      <include name="*.html"/> 
      <include name="*.jsp" /> 
      <include name="*.gif" /> 
    </fileset> 
  </copy> 
</target>

</project>
